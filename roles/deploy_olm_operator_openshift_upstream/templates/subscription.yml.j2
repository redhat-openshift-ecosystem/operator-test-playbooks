apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ package_name }}
  namespace: {{ openshift_namespace }}
spec:
  name: {{ package_name }}
  source: test-operators-ocs
  sourceNamespace: openshift-marketplace
  channel: "{{ current_channel }}"
