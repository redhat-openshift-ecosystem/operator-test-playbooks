apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: test-operators
{% if operator_allnamespaces_support != true %}
spec:
  targetNamespaces:
  {% if operator_ownnamespace_support == true %}
    - {{ openshift_namespace }}
  {% elif operator_singlenamespace_support == true %}
    - default
  {% elif operator_multinamespace_support == true %}
    - {{ openshift_namespace }}
    - default
  {% endif %}
{% endif %}