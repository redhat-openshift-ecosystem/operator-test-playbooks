apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ package_name }}-test
  namespace: {{ operator_suggested_namespace }}
  labels:
    operator: test
spec:
  name: {{ package_name }}
  source: test-operators-ocs
  sourceNamespace: {{ operator_suggested_namespace }}
  channel: "{{ current_channel }}"