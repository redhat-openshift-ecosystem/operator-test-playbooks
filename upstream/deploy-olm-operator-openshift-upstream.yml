---
- name: Deploy on OCP
  hosts: all
  become: false
  gather_facts: false

  tasks:

    - name: "Load operator info from file"
      include_role:
        name: operator_info_load

    - name: "Deploy on OCP"
      include_role:
        name: deploy_olm_operator_openshift_upstream
